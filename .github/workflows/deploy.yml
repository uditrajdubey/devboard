# GOAL: i have to deploy the project using git hum action 

name: Deploy the project

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v7

      - name: Run docker container
        run: docker compose up -d 